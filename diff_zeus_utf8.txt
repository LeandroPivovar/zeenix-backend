diff --git a/src/autonomous-agent/strategies/zeus.strategy.ts b/src/autonomous-agent/strategies/zeus.strategy.ts
index 7ec50de..9a57b77 100644
--- a/src/autonomous-agent/strategies/zeus.strategy.ts
+++ b/src/autonomous-agent/strategies/zeus.strategy.ts
@@ -257,8 +257,7 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
          LEFT JOIN user_settings s ON c.user_id = s.user_id
          WHERE c.is_active = TRUE 
            AND c.agent_type = 'zeus'
-           AND c.session_status NOT IN ('stopped_profit', 'stopped_loss', 'stopped_blindado', 'stopped_consecutive_loss')`,
-
+           AND c.session_status NOT IN ('stopped_profit', 'stopped_loss', 'stopped_blindado')`,
             );
 
             for (const user of activeUsers) {
@@ -281,7 +280,7 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
                                 if (entry) resolvedToken = entry[1] as string;
                             }
                         } catch (e) {
-                            this.logger.warn(`[Zeus][${userId}] Erro ao fazer parsing do deriv_raw para fallback de token: ${e.message}`);
+                            this.logger.warn(`[Zeus][${userId}]Erro ao fazer parsing do deriv_raw para fallback de token: ${e.message} `);
                         }
                     }
                 } else {
@@ -297,23 +296,23 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
                                 if (entry) resolvedToken = entry[1] as string;
                             }
                         } catch (e) {
-                            this.logger.warn(`[Zeus][${userId}] Erro ao fazer parsing do deriv_raw para fallback de token (Real): ${e.message}`);
+                            this.logger.warn(`[Zeus][${userId}] Erro ao fazer parsing do deriv_raw para fallback de token(Real): ${e.message} `);
                         }
                     }
                 }
 
                 // Log para debug da resolu├º├úo - DETALHADO POR SOLICITA├ç├âO DO USU├üRIO
                 this.logger.log(`[Zeus][${userId}] ­ƒöì Rastreio de Token:
-                    - Config Token: ${user.config_token ? user.config_token.substring(0, 8) + '...' : 'N/A'}
-                    - Trade Currency (Settings): ${user.trade_currency}
-                    - Want Demo: ${wantDemo}
-                    - Token Demo (User): ${user.token_demo ? user.token_demo.substring(0, 8) + '...' : 'N/A'}
-                    - Token Real (User): ${user.token_real ? user.token_real.substring(0, 8) + '...' : 'N/A'}
-                    - Resolved Token: ${resolvedToken ? resolvedToken.substring(0, 8) + '...' : 'N/A'}
-                `);
+            - Config Token: ${user.config_token ? user.config_token.substring(0, 8) + '...' : 'N/A'}
+            - Trade Currency(Settings): ${user.trade_currency}
+            - Want Demo: ${wantDemo}
+            - Token Demo(User): ${user.token_demo ? user.token_demo.substring(0, 8) + '...' : 'N/A'}
+            - Token Real(User): ${user.token_real ? user.token_real.substring(0, 8) + '...' : 'N/A'}
+            - Resolved Token: ${resolvedToken ? resolvedToken.substring(0, 8) + '...' : 'N/A'}
+            `);
 
                 if (resolvedToken !== user.config_token) {
-                    this.logger.log(`[Zeus][ResolucaoToken] User ${userId}: Token atualizado dinamicamente. Modo=${wantDemo ? 'DEMO' : 'REAL'}.`);
+                    this.logger.log(`[Zeus][ResolucaoToken] User ${userId}: Token atualizado dinamicamente.Modo = ${wantDemo ? 'DEMO' : 'REAL'}.`);
                 }
 
                 // Ô£à Map Risk Profile from DB/Frontend to Enum
@@ -371,15 +370,15 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
                     this.initializeUserState(userId, zeusConfig);
                     // Ô£à [ZENIX v4.2] RECONSTRU├ç├âO AUTOM├üTICA EM REIN├ìCIOS
                     this.reconstructStateFromHistory(userId).catch(e => {
-                        this.logger.error(`[Zeus][${userId}] Erro na reconstru├º├úo autom├ítica: ${e.message}`);
+                        this.logger.error(`[Zeus][${userId}] Erro na reconstru├º├úo autom├ítica: ${e.message} `);
                     });
                 }
 
                 // Ô£à Log de sucesso (apenas na primeira vez/reconex├úo)
-                this.logger.log(`[Zeus] Ô£à Usu├írio sincronizado: ${userId} (${user.email || 'N/A'}) - Perfil: ${zeusConfig.riskProfile}`);
+                this.logger.log(`[Zeus] Ô£à Usu├írio sincronizado: ${userId} (${user.email || 'N/A'}) - Perfil: ${zeusConfig.riskProfile} `);
             }
         } catch (error) {
-            this.logger.error(`[Zeus] ÔØî Erro ao sincronizar usu├írios: ${error.message}`);
+            this.logger.error(`[Zeus] ÔØî Erro ao sincronizar usu├írios: ${error.message} `);
         }
     }
 
@@ -484,16 +483,16 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
             const trades = await this.dataSource.query(
                 `SELECT status, profit_loss, created_at 
                  FROM autonomous_agent_trades 
-                 WHERE user_id = ? 
-                   AND strategy = 'zeus' 
+                 WHERE user_id = ?
+                AND strategy = 'zeus' 
                    AND created_at >= ?
-                 ORDER BY created_at DESC 
+                ORDER BY created_at DESC 
                  LIMIT 100`,
                 [userId, sessionDate]
             );
 
             if (!trades || trades.length === 0) {
-                this.logger.debug(`[Zeus][${userId}] ­ƒöì Hist├│rico vazio para esta sess├úo (${sessionDate.toISOString()}).`);
+                this.logger.debug(`[Zeus][${userId}] ­ƒöì Hist├│rico vazio para esta sess├úo(${sessionDate.toISOString()}).`);
                 state.mode = 'NORMAL';
                 state.consecutiveLosses = 0;
                 state.profit = config.dailyProfit || 0;
@@ -568,10 +567,10 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
                 state.recoveryLock = false;
             }
 
-            this.logger.log(`[Zeus][${userId}] Ô£à ESTADO RECONSTRU├ìDO: Modo=${state.mode}, Lucro=$${state.profit.toFixed(2)}, Perdas=${state.consecutiveLosses}`);
+            this.logger.log(`[Zeus][${userId}] Ô£à ESTADO RECONSTRU├ìDO: Modo = ${state.mode}, Lucro = $${state.profit.toFixed(2)}, Perdas = ${state.consecutiveLosses} `);
             this.saveLog(userId, 'INFO', 'CORE', `­ƒöä ESTADO RECUPERADO: Lucro $${state.profit.toFixed(2)} | Retomando em modo ${state.mode} com ${consecutiveLosses} perdas consecutivas.`);
         } catch (error) {
-            this.logger.error(`[Zeus][${userId}] ÔØî Erro ao reconstruir estado:`, error);
+            this.logger.error(`[Zeus][${userId}] ÔØî Erro ao reconstruir estado: `, error);
         }
     }
 
@@ -643,11 +642,11 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
             if (!hasSignificantChange) {
                 // Se n├úo mudou nada importante, apenas mant├®m o config e o estado atual
                 this.userConfigs.set(userId, zeusConfig);
-                this.logger.debug(`[Zeus][${userId}] ­ƒöä Sync: Configura├º├Áes inalteradas. Mantendo estado de mem├│ria.`);
+                this.logger.debug(`[Zeus][${userId}] ­ƒöä Sync: Configura├º├Áes inalteradas.Mantendo estado de mem├│ria.`);
                 return;
             }
 
-            this.logger.log(`[Zeus][${userId}] ­ƒöä Mudan├ºa de configura├º├úo detectada. Atualizando...`);
+            this.logger.log(`[Zeus][${userId}] ­ƒöä Mudan├ºa de configura├º├úo detectada.Atualizando...`);
             this.userConfigs.set(userId, zeusConfig);
 
             // Se mudou algo significativo, preservamos o que der do estado mas revalidamos
@@ -679,11 +678,11 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
 
         // Ô£à PR├ë-AQUECER conex├úo WebSocket para evitar erro "Conex├úo n├úo est├í pronta"
         try {
-            this.logger.log(`[Zeus][${userId}] ­ƒöî Pr├®-aquecendo conex├úo WebSocket...`);
+            this.logger.log(`[Zeus][${userId}] ­ƒöî Pr├® - aquecendo conex├úo WebSocket...`);
             await this.warmUpConnection(zeusConfig.derivToken);
-            this.logger.log(`[Zeus][${userId}] Ô£à Conex├úo WebSocket pr├®-aquecida e pronta`);
+            this.logger.log(`[Zeus][${userId}] Ô£à Conex├úo WebSocket pr├® - aquecida e pronta`);
         } catch (error: any) {
-            this.logger.warn(`[Zeus][${userId}] ÔÜá´©Å Erro ao pr├®-aquecer conex├úo (continuando mesmo assim):`, error.message);
+            this.logger.warn(`[Zeus][${userId}] ÔÜá´©Å Erro ao pr├® - aquecer conex├úo(continuando mesmo assim): `, error.message);
         }
 
         // Ô£à Obter modo do estado (inicializado como 'NORMAL')
@@ -710,7 +709,7 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
             agentName: 'Zeus'
         });
 
-        this.logger.log(`[Zeus] Ô£à Usu├írio ${userId} ativado | Symbol: ${zeusConfig.symbol} | Total configs: ${this.userConfigs.size}`);
+        this.logger.log(`[Zeus] Ô£à Usu├írio ${userId} ativado | Symbol: ${zeusConfig.symbol} | Total configs: ${this.userConfigs.size} `);
     }
 
     async deactivateUser(userId: string): Promise<void> {
@@ -729,7 +728,7 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
             if (!otherUsersWithSameToken) {
                 const conn = this.wsConnections.get(token);
                 if (conn) {
-                    this.logger.log(`[Zeus] ­ƒöî Fechando conex├úo WebSocket (Token: ${token.substring(0, 8)}...) - Nenhum usu├írio ativo.`);
+                    this.logger.log(`[Zeus] ­ƒöî Fechando conex├úo WebSocket(Token: ${token.substring(0, 8)}...) - Nenhum usu├írio ativo.`);
                     if (conn.keepAliveInterval) clearInterval(conn.keepAliveInterval);
                     conn.ws.close();
                     this.wsConnections.delete(token);
@@ -755,7 +754,7 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
         const tickSymbol = symbol || 'R_100'; // Ô£à Todos os agentes aut├┤nomos usam R_100
 
         // Ô£à Log de debug para verificar se est├í recebendo ticks
-        // this.logger.debug(`[Zeus] ­ƒôÑ Tick recebido: symbol=${tickSymbol}, value=${tick.value}, users=${this.userConfigs.size}`);
+        // this.logger.debug(`[Zeus] ­ƒôÑ Tick recebido: symbol = ${ tickSymbol }, value = ${ tick.value }, users = ${ this.userConfigs.size } `);
 
         if (this.userConfigs.size === 0) {
             // this.logger.warn(`[Zeus] ÔÜá´©Å Tick recebido mas nenhum usu├írio configurado.`);
@@ -765,16 +764,16 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
         // Ô£à Processar para todos os usu├írios ativos
         for (const [userId, config] of this.userConfigs.entries()) {
             // Ô£à Log tempor├írio para debug de match
-            // this.logger.debug(`[Zeus] Checking match: TickSymbol=${tickSymbol} vs UserSymbol=${config.symbol}`);
+            // this.logger.debug(`[Zeus] Checking match: TickSymbol = ${ tickSymbol } vs UserSymbol = ${ config.symbol } `);
 
             if (this.isSymbolMatch(tickSymbol, config.symbol)) {
                 promises.push(this.processTickForUser(userId, tick).catch((error) => {
-                    this.logger.error(`[Zeus][${userId}] Erro ao processar tick:`, error);
+                    this.logger.error(`[Zeus][${userId}] Erro ao processar tick: `, error);
                 }));
             } else {
                 // Log mismatch only once per 100 ticks to avoid spam but allow debugging
                 if (Math.random() < 0.01) {
-                    this.logger.warn(`[Zeus][DEBUG] Symbol Mismatch: Tick=${tickSymbol} User=${config.symbol}`);
+                    this.logger.warn(`[Zeus][DEBUG] Symbol Mismatch: Tick = ${tickSymbol} User = ${config.symbol} `);
                 }
             }
         }
@@ -823,7 +822,9 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
         if (isHigh) {
             return { passes: true, metrics: { sequence }, count };
         }
-        return { passes: false, reason: `Onda Alta: ${count}/4 d├¡gitos altos [${sequence.join(', ')}]`, count };
+        return {
+            passes: false, reason: `Onda Alta: ${count}/4 d├¡gitos altos [${sequence.join(', ')}]`, count
+        };
     }
 
     /**
@@ -1898,7 +1899,7 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
                 this.saveLog(userId, 'SUCCESS', 'SESSION', `­ƒÅå SESS├âO FINALIZADA (${state.cycleCurrent} CICLOS COMPLETOS)`);
                 state.sessionEnded = true;
                 state.endReason = 'TARGET';
-                await this.handleStopCondition(userId, 'TAKE_PROFIT');
+                await this.handleStopCondition(userId, 'CYCLE_COMPLETE');
             }
             return;
         }
@@ -2155,13 +2156,17 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
                 message = `STOP LOSS ATINGIDO! resultado_total=${state.lucroAtual >= 0 ? '+' : ''}${state.lucroAtual.toFixed(2)}, limite=${config.dailyLossLimit.toFixed(2)} | cycle=${state.cycleCurrent}. Encerrando opera├º├Áes.`;
                 break;
             case 'CONSECUTIVE_LOSS':
-                status = 'stopped_consecutive_loss';
+                status = 'stopped_loss';
                 message = `­ƒøæ STOP POR PERDAS CONSECUTIVAS! Mercado Inst├ível. Opera├º├Áes encerradas para prote├º├úo do capital. | Resultado: ${state.lucroAtual >= 0 ? '+' : ''}${state.lucroAtual.toFixed(2)} | cycle=${state.cycleCurrent}.`;
                 break;
             case 'BLINDADO':
                 status = 'stopped_blindado';
                 message = `STOP LOSS BLINDADO ATINGIDO! Saldo caiu para $${((config.initialBalance || 0) + state.lucroAtual).toFixed(2)} | cycle=${state.cycleCurrent}. Encerrando opera├º├Áes do dia.`;
                 break;
+            case 'CYCLE_COMPLETE':
+                status = 'stopped_profit';
+                message = `SESS├âO FINALIZADA: Todos os ${ZEUS_CONSTANTS.cycles} ciclos foram conclu├¡dos com sucesso! Lucro Total: $${state.profit.toFixed(2)}.`;
+                break;
 
         }
 
@@ -2202,6 +2207,7 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
         const analysisData = {
             strategy: 'zeus',
             mode: state.mode,
+            sessionId: config.sessionId, // Ô£à [ZENIX v4.2] Associar trade ├á sess├úo
             probability: trade.marketAnalysis.probability,
             signal: trade.marketAnalysis.signal,
             volatility: trade.marketAnalysis.details?.volatility,
@@ -2218,12 +2224,13 @@ export class ZeusStrategy implements IAutonomousAgentStrategy, OnModuleInit {
         try {
             const result = await this.dataSource.query(
                 `INSERT INTO autonomous_agent_trades (
-          user_id, analysis_data, confidence_score, analysis_reasoning,
+          user_id, session_id, analysis_data, confidence_score, analysis_reasoning,
           contract_type, contract_duration, entry_price, stake_amount,
           martingale_level, payout, symbol, status, strategy, deriv_token, deriv_account_type, created_at
-        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'PENDING', 'zeus', ?, ?, NOW())`,
+        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'PENDING', 'zeus', ?, ?, NOW())`,
                 [
                     userId,
+                    config.sessionId || null, // Ô£à [ZENIX v4.2] Salvar session_id do config
                     JSON.stringify(analysisData),
                     trade.marketAnalysis.probability,
                     analysisReasoning,
